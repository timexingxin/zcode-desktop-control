{
  "name": "@zcode-community/core",
  "version": "0.1.0",
  "description": "Core models, stable handles, state compression, and error taxonomy for Computer Use.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "license": "Apache-2.0"
}
